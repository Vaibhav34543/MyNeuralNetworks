# MyNeuralNetworks
The `neural_network_1.py` file is a neural network containing a **single neuron**. Not really sure if we can call it a network lmao. It was designed to predict that the output is 10x times the input, and here's the weight and bias values after 15 iterations (not averaged).

<img width="494" height="501" alt="image" src="https://github.com/user-attachments/assets/6be2ff20-cd09-4e58-8be3-01da132c2ca9" /><br>
For the `neural_network_2.py` the dataset has been made to be multiple numbers (lists) and also the function is a proper `y = mx + c` linear function, in this case it is `-2x + 5`

<img width="455" height="147" alt="image" src="https://github.com/user-attachments/assets/df50f75c-3f7c-466b-9715-91357b550ab4" />
